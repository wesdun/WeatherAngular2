import {
    beforeEach,
    beforeEachProviders,
    describe,
    expect,
    it,
    inject,
    injectAsync
} from 'angular2/testing';

describe('weather', () => {
    it('true is trues', () => {
        expect(true).toEqual(true);
    });
});
